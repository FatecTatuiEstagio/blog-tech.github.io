# Git LFS - Large File Storage para arquivos de áudio
# Esses arquivos serão armazenados eficientemente no Git LFS

# Áudios dos posts (binários, sem conversão de line endings)
assets/audio/posts/*.mp3 filter=lfs diff=lfs merge=lfs -text binary
assets/audio/posts/*.wav filter=lfs diff=lfs merge=lfs -text binary
assets/audio/posts/*.ogg filter=lfs diff=lfs merge=lfs -text binary
assets/audio/posts/*.m4a filter=lfs diff=lfs merge=lfs -text binary

# Prevenir problemas com line endings em arquivos de texto
* text=auto
*.mp3 binary
*.wav binary
*.ogg binary
*.m4a binary
*.md text
*.yml text
*.yaml text
*.json text
*.html text
*.css text
*.js text
*.rb text
